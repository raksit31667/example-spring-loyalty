apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-spring-loyalty.fullname" . }}
  labels:
    {{- include "example-spring-loyalty.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-spring-loyalty.selectorLabels" . | nindent 4 }}
